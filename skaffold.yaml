---
apiVersion: skaffold/v1alpha2
kind: Config
build:
  artifacts:
  - imageName: teddyyy/grpc-web-proxy
    docker:
deploy:
  kubectl:
    manifests:
      - k8s/*

profiles:
  - name: prd
    ## This profile runs unit tests and builds the image
    build:
      local: {skipPush: false}
      artifacts:
        - imageName: teddyyy/grpc-web-proxy
          docker:
    deploy: {}  # not needed here as such
