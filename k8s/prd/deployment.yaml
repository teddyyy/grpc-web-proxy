apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    run: grpc-web-proxy
  name: grpc-web-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      run: grpc-web-proxy
  strategy: {}
  template:
    metadata:
      labels:
        run: grpc-web-proxy
    spec:
      containers:
      - image: asia.gcr.io/agdc-k8s/grpc-web-proxy:a151be90772c40092ae443b364da1f58bc48a0b0
        name: grpc-web-proxy
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          timeoutSeconds: 60
          initialDelaySeconds: 30
          periodSeconds: 30
status: {}
